<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.finderlite"
          android:versionCode="1"
          android:versionName="1.0.0">

	<uses-sdk android:minSdkVersion="21"
              android:targetSdkVersion="33" />

	<!-- ── Permissions ── -->
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />

	<!-- Required on Android 13 (API 33) to post notifications -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

	<!-- ── Self-update permissions ── -->
	<!-- Required on Android 8+ to install APKs from this app -->
	<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
	<!-- Allows replacing the existing installation -->
	<uses-permission android:name="android.permission.REPLACE_EXISTING_PACKAGE" />

	<!-- ── Package visibility (Android 11+) ──
         Lets us launch the system Package Installer activity. -->
	<queries>
		<intent>
			<action android:name="android.intent.action.VIEW" />
			<data android:mimeType="application/vnd.android.package-archive" />
		</intent>
	</queries>

	<application android:label="Finder Lite"
                 android:icon="@mipmap/icon"
                 android:roundIcon="@mipmap/icon_round"
                 android:theme="@style/MainTheme"
                 android:allowBackup="true">

		<!-- ── Foreground service ── -->
		<service android:name="com.finderlite.BackgroundLocationService"
                 android:foregroundServiceType="location"
                 android:enabled="true"
                 android:exported="false"
                 tools:node="merge" />

		<!-- ── FileProvider for APK install (API 24+) ── -->
		<provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.finderlite.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
			<meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
		</provider>

	</application>
</manifest>